// Base build configuration (safe to commit)
// All build settings are now defined directly in Dev.xcconfig, Acc.xcconfig, and Production.xcconfig
// This file is kept as a reference for common settings across all configurations.
